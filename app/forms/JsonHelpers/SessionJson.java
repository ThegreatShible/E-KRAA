package forms.JsonHelpers;

import models.session.Session;

public class SessionJson {

    public static SessionJson fromSession(Session session) {
        SessionJson sessionJson = new SessionJson();
        return sessionJson;
    }
}
